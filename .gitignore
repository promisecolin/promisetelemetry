node_modules/
build/
dist/
coverage/
.DS_Store
*.log
*.zip
.env*
!.env.example
!monitoring/.env.example
monitoring/.env
grafana-data/
prometheus-data/

# Sensitive deployment credentials
DEPLOYMENT.md
aws/

# Local Claude Code settings (contains infra fingerprints from prior sessions)
.claude/settings.local.json

# Unrelated large binaries, not part of the app backup
FINAL YEAR REPORT FINAL/

# Local-only compose overrides for running everything on one machine
docker-compose.local.yml
monitoring/docker-compose.local-override.yml
docker-compose.tunnel.yml
